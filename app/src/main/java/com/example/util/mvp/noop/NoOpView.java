package com.example.util.mvp.noop;

import com.hannesdorfmann.mosby3.mvp.MvpView;

/**
 * An empty view interface for screens that require no such interface. (There should really be no screen like that in a normal project, but
 * this is a test project).
 */
public interface NoOpView extends MvpView {

}
